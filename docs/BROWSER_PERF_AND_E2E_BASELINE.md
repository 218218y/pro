# Browser perf and E2E baseline

Tool-owned report target for `npm run perf:browser:update-baseline` and browser perf smoke runs.

The old long report was removed from active docs. Fresh browser metrics should be regenerated by the browser perf tool when needed.

## Current policy

- Use `npm run e2e:smoke` for critical browser journey proof.
- Use `npm run perf:browser` for browser perf enforcement.
- Use `.artifacts/browser-perf/latest.*` for current run output.
