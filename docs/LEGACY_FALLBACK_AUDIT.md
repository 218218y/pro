# Legacy Fallback Audit

Placeholder for `npm run report:legacy-fallbacks`.
The active fallback policy lives in `docs/QUALITY_GUARDRAILS.md`, and the current count-lock lives in `tools/wp_legacy_fallback_allowlist.json`.
