# Modernization decision gate

The numbered refactor track is closed. This file is the current decision gate for future modernization; it is not a stage diary.

## Current control plane

- `tools/wp_contract_registry.mjs` registers each canonical architecture contract with one owner, one package lane, its scope, and focused behavior evidence.
- `tools/wp_private_owner_import_boundary_audit.mjs` enforces registered facade/private-owner families through one data-driven import audit.
- The same audit owns identity-only private-wrapper topology. Deliberate facades need an explicit registry justification; reviewed single-consumer wrappers live in `tools/wp_identity_facade_inventory.json` with their exact importer, and dead wrappers are removed rather than preserved as compatibility archaeology.
- `tools/wp_test_portfolio_audit.mjs` owns test reachability, group integrity, historical-stage rejection, overlap mapping, and the rule that large direct package test lists must move into the canonical group catalog.
- Capability-named runtime and ownership tests preserve current behavior. Historical `refactor_stage*` proof files are not an accepted substitute. Phase 5 control-plane cleanup is complete: historical proof files are absent, dead identity wrappers are absent, current wrapper topology is explicit, large named test lanes are catalog-backed, runner metadata is canonical without a second `environment` field that can drift, and verify lanes compose their shared type/layer/API guard tail through one helper instead of copying the same task suffix across the catalog.
- Performance observability is on browser schema 28: dev and release regression baselines are bound to separate measurement profiles, comparable material drift and dominant-source ratios are separated from sub-20ms timing noise and mixed recovery outcomes, interaction-wait-dominated spans are reported without duplicating customer-journey and code-execution gates, CLS/LCP/Long Task count-total-p95/render-settle are measured, and INP is estimated from Event Timing interactions with a first-input fallback. Dev React module loading is reported as `boot.browser.react-module-load`, outside the app-owned `boot.browser.setup` span. Dev regression keeps a 500ms INP ceiling while the product and release targets remain 200ms.
- Final evidence is now part of the release contract rather than an optional afterthought: the default closeout includes browser performance, CLS/LCP/INP must be genuinely measured rather than inferred from zero values, and an environment-blocked browser lane prevents generation of a clean final verification report while remaining distinct from a code failure.
- Error-observability modernization is complete for Phase 3. The canonical Oxc-based silent-catch policy owns three exact current-state ratchets. Their mutable counts are generated in `docs/MODERNIZATION_STATE.md` instead of being copied into this living plan; the invariant remains **0 bare catches globally** and **0 vague `// ignore` / `// swallow` classifications**. History operations, restore-state reads, timer cleanup, boot command finalizers, boot seed config/action/write fallbacks, the Autosave snapshot/storage/scheduling pipeline, camera access/motion, Saved Models clone/normalization/persistence, Config Compounds convergence, and the complete Edit State reset/synchronization path now report nonfatal owner failures while preserving deliberate fail-soft behavior. Autosave continues from a rejected Project Capture result to the canonical History snapshot fallback, camera motion continues through vector-copy, manual-interpolation, and timer scheduling fallbacks, and Saved Models cloning fails closed rather than returning a live source object when every detached-copy strategy rejects. Saved Models loading consumes one canonical envelope snapshot and coalesces repairs into one locked transaction. Config Compounds requires both writes plus concrete snapshot verification. Edit State reset exposes real convergence to project/kernel/UI callers while still attempting independent cleanup; React and native UI mode transitions fail closed before door, chrome, or callback side effects when canonical mode/reset writes are rejected. Dimension synchronization commits builder/runtime mirrors together, rolls back the builder mirror on a rejected runtime write, and immediately flushes when timer scheduling is unavailable. Cloud Sync scheduled pulls now preserve coalescing while reporting timer and execution failures, remote adoption schedules explicit recovery after rejected commits, conflict resolution fails closed until a durable resolved record or tombstone exists, and public subscribers receive detached snapshots. Render/Canvas commit hardening now prevents history-batch callback replay, rolls camera pose back after rejected camera/control writes, treats renderer setup failure as surface-creation failure, reports committed measurement-state failures, and refreshes Canvas paint only after the canonical config mutation actually completes. Specialized Canvas module authoring now also routes every `patchForStack` write through one transactional structural-commit seam that detects unavailable/rejected writers, prevents partial mutation commits, preserves stable nested identities on success, and gates success-side hover/toast/motion effects on an accepted commit. Platform/UI boot convergence is now hardened as well: failed UI boot sessions reset their init guard for repair-and-retry, platform boot never publishes ready/done after a failed entry, ready-state publication keeps Autosave disabled when `systemReady` is rejected, and Primary Mode mirror failures are diagnostic instead of silent.

## Phase 3 completion map

Phase 3 is completed by capability risk, not by mechanically eliminating every catch. The remaining work is ordered as follows:

1. **Saved Models persistence and collection transactions — complete.** Storage loading reads one canonical envelope, normalization repairs are recomputed under the collection lock and coalesced into one transaction, failed durable writes leave runtime state and notifications unchanged, mirror failures and PDF-state reads publish stable diagnostics, and listeners/runtime mirrors receive detached snapshots.
2. **Configuration and edit-state mutation — complete.** The `config_compounds_*` family verifies both compound writes against one concrete snapshot, clears settled in-flight attempts, and reports rejected writes or scheduling failures. The `edit_state_*` family now exposes a real reset result, keeps independent cleanup running after an owner rejection, blocks kernel, React, and native UI mode transitions when reset convergence fails, prevents rejected native transitions from publishing door/chrome/callback side effects, coordinates builder/runtime dimension commits behind one timer, rolls the builder mirror back after a rejected runtime write, and immediately commits when scheduling is unavailable. Optional DOM reads remain fallback-only and are not treated as durable-state failures.
3. **Cloud Sync persistence and reconciliation — complete.** Scheduled pulls preserve coalescing while reporting timer, diagnostic, synchronous, and asynchronous execution failures; a rejected timer falls through to an immediate pull instead of losing queued work. Remote reads, seed writes, hash computation, and adoption failures publish stable diagnostics and schedule the correct pull/push recovery. Conflict resolution now closes only after either a durable resolved-state record or a durable tombstone exists; if both persistence paths fail, the conflict remains active and resolution fails closed. Collection observers and status subscribers receive independently detached snapshots, and initial-pull diagnostics cannot suppress canonical nonfatal reporting. Remaining Cloud Sync statement-free catches are reviewed observer isolation, detach cleanup, exotic-object fallback, or feedback fallback paths rather than silent durable-state transitions.
4. **Render, measurement, and Canvas commit paths — complete.** History batching no longer replays a mutation after canonical execution has started; paint refresh/render occurs only after the config mutation completes; camera pose writes roll back to the prior snapshot if controls/projection update fails; renderer setup fails closed; render-scheduler follow-through reports through the canonical platform rejection path; and committed measurement-mode/render failures publish stable diagnostics. Specialized structural authoring now has one canonical `patchForStack` seam with detached-draft mutation, rollback on writer rejection/throw, stable nested-identity reconciliation, and fail-closed success-side effects. Direct drawer-hit/manual-layout orchestration failures are observable. Hover preview, disposal, shader warmup, and optional visual cleanup remain reviewed best-effort.
5. **Boot/install and UI state mirrors — complete.** Platform boot now publishes `initDone`/`bootReady` only after the canonical start entry actually succeeds, keeps failed attempts retryable, and falls through scheduler failures without replaying a failed boot callback. UI boot aborts reset `didInit` so repaired wiring can retry instead of being permanently short-circuited, and aborts force `systemReady`/Autosave back to the safe state. The ready timer enables Autosave only after the canonical `systemReady=true` write is accepted. Primary Mode DOM/store mirrors report failed reads/subscriptions/render writes instead of silently leaving stale visual state. Optional browser capability detection and cleanup remain best-effort.
6. **Classification closeout — complete.** Production has zero bare catches and zero vague `// ignore` / `// swallow` catch comments in every layer. Runtime state/access fallbacks now report rejected Boot entry lookup, Internal boot-state attachment, Doors runtime writes, Cache mutations, Render cache/material writes, and failed door/drawer scans where functional state could be lost; reviewed browser/config/render-slot fallbacks carry precise capability-specific rationale. Platform installation no longer publishes installed state after required Tools runtime installation fails, drawer forced-close write failures are observable, and reviewed timer/debug/cache/disposal/subscription/clock fallbacks are explicitly classified. Browser adapters are fully classified as DOM/capability/cleanup best-effort where appropriate. Phase 3 therefore ends with every remaining statement-free catch classified by capability, while the exact current inventory is generated in `docs/MODERNIZATION_STATE.md`; bare catches and vague catch comments remain locked to zero.

Phase 3 is formally closed. **Canonical Domain Codecs are now closed as well** across Saved Models, Cloud Collections, Project Config maps, Settings Backup, and the full current Project Schema. Each persistence boundary now routes validation/normalization through one domain owner, exposes clone/serialize/fingerprint behavior, and is guarded by `check:domain-codecs` plus deterministic round-trip/malformed runtime coverage. Settings Backup exports schema v1 and explicitly migrates legacy unversioned backups; unsupported future backup versions, unsupported Cloud Collections schemas, and historical Project Schema versions fail closed. **Cloud Sync state-machine decomposition is now closed** behind the unchanged `createCloudSyncOwnerGatewayIo` surface: credential/session lifecycle, gateway transport, row cache, conflict journal, conflict resolution, and remote adoption have independent state/lifecycle owners, while browser lock acquisition remains at the runtime-access boundary and is dependency-injected into conflict resolution. `check:cloud-sync-owner-decomposition` and the focused gateway runtime suite guard that ownership. **Targeted E2E profile expansion is closed**, and **Contract Engine modernization is now consolidated around twelve Dimension Composition owners and five static policies**. Core Carcass, Chest Mode Inputs, Render Loop Door Motion, Runtime Default State, and Core Module Layout no longer retain separate source-shape ownership files; five redundant guards were removed while their behavior remains covered by direct runtime matrices. The declarative engine now also supports literal-only frozen policies with an empty provenance source list, while still enforcing exact shape, consumer inventory, member-only use, aliases, provenance, and composition-owner routing where applicable. **TypeScript/control-plane simplification is also closed**: the whole-project strict config is the canonical type source of truth, `ui-lean` is the only alternate portability lane, and the redundant per-layer strict/non-strict matrix is gone from local scripts, verify lanes, and CI. **Viewer Measurement Capability DI is now closed across the full feature boundary**: the resolution cores retain the four-capability geometry runtime, the public tool is a thin `AppContainer` facade, the click/hover/mode flow consumes grouped state/render/UI/diagnostic capabilities only, and one runtime adapter owns all direct runtime access. Focused tests execute the flow without an application object, while architecture lint prevents direct runtime/AppContainer access from returning to the flow or direct runtime imports from returning to the facade. **The evidence-driven Canvas Capability DI pilot now also covers Door Layout Alignment and Cell Dimensions Free Box hover**: each actively changed hover core accepts a narrow cached capability surface, keeps `AppContainer` in one runtime adapter, has direct no-application behavior coverage, and is protected by architecture lint against runtime/kernel or container leakage. Typed Geometry / Build IR now covers the main-carcass shell/cornice geometry **and the complete corner-cornice family**. `carcass_cornice_ir.ts` and `carcass_shell_ir.ts` own the main carcass; `corner_cornice_ir.ts` is the canonical wing/connector contract; `corner_wing_cornice_plan.ts` and `corner_connector_cornice_plan.ts` compute deterministic profile/wave/box operations before Three.js; `corner_cornice_profile_plan.ts` owns shared profile construction; and `corner_cornice_render.ts` is the single emission boundary. The four former wing/connector profile/wave emitters are render-only wrappers, duplicate profile/miter construction and dynamic runtime readers are removed, and malformed plans fail closed before emission. **Canvas part-hover preview protocol is now closed as well** for the shared box/object-box family: `canvas_picking_part_hover_preview_protocol.ts` owns the typed `box`/`object_boxes` command and finite/positive validation, while `canvas_picking_part_hover_preview_runtime.ts` is the only adapter in this family that probes `setSketchPlacementPreview` and adds `App`/`THREE`. Generic paint hover, removable-part hover, and Cell Dimensions hover now emit typed commands through that runtime; duplicated cleanup/probing was removed, and architecture lint prevents those clients from regaining `UnknownRecord`, `THREE`, or direct RenderOps preview access. Ext-drawer/divider/manual-layout modes keep their existing specialized protocols because forcing drawer-motion or manual-layout fields into the part-hover envelope would broaden the contract rather than simplify it. Further Typed IR, Capability DI, or Contract Engine work is evidence-driven only; do not open another modernization lane without an actively changed family and a measurable ownership, dynamic-record, or source-shape problem. **Planar Reflector lifecycle ownership is now closed at the proven seams**: `planar_reflector_state.ts` owns canonical mirror/state parsing, `planar_reflector_warm_cache.ts` owns warm-target reuse/disposal, and `planar_reflector_refresh_runtime.ts` owns progressive refresh budgeting, retry/backoff, tracked stats, and render-pass attempts. `planar_reflector_runtime.ts` remains the public installation/material/cube-fallback owner and re-exports lifecycle APIs; it no longer imports the render pass or camera/renderer/scene accessors. The audit did not show a measured render-cost regression, so shader, target sizing, sliding occlusion, surface installation, and cube-mode restoration were deliberately left intact rather than split by file size.

## Current evidence queue

The post-pilot audit keeps future work ordered by proven risk rather than file size alone:

1. **Contract Engine expansion — evidence only.** Two families are closed. Reuse the declarative engine again only when another actively changed source/hash-heavy family proves implementation-shape friction; no broad migration campaign is planned.
2. **Specialized Canvas preview families — only on evidence.** Ext-drawer/divider/manual-layout previews carry specialized drawer-motion or authoring payloads and should not be forced into the closed part-hover envelope. Revisit only when one of those families is actively changed and duplicate probing or a concrete defect can be removed without widening the generic contract.
3. **Planar reflector installation/material path — observe only.** The lifecycle seams are now separated; do not split the remaining shader/material/surface/cube-fallback owner unless a measured render regression, recurring defect, or second independent ownership seam appears.

This queue is a decision aid, not a mandate to refactor every item. A product bug or measured regression outranks speculative cleanup.

## Professional change gate

A structural change is justified only when it meets all of these conditions:

1. It removes a real ownership mix, duplicated path, measurable cost, or verified regression risk.
2. The canonical consumer contract is known before editing.
3. Behavior coverage remains focused on observable results; architecture coverage remains focused on imports, surfaces, and ownership.
4. A new facade is introduced only when it protects a deliberate API, service/family entry, environment boundary, or stable multi-consumer seam.
5. The result reduces future change cost rather than moving the same logic into more files.

Do not split by line count, create wrapper aliases for obsolete names, or add another test lane that reruns the canonical runtime suite. If a historical proof and a current contract protect the same invariant, keep the current contract and remove the historical proof.

## Validation matrix

| Change                        | Minimum focused validation                                                       |
| ----------------------------- | -------------------------------------------------------------------------------- |
| Private owner/facade topology | `check:contract-registry`, `check:private-owner-imports`, related behavior tests |
| Builder/render behavior       | focused runtime tests, canonical `typecheck`, relevant architecture contract     |
| Runtime/API hardening         | focused runtime/API tests, canonical `typecheck`, public-surface contract        |
| Project import/load ingress   | project fixture and migration-boundary checks                                    |
| Cloud Sync lifecycle          | focused race/timer tests and contracts                                           |
| Canvas pointer identity       | canvas identity/parity contracts and focused runtime tests                       |
| React UI behavior             | focused UI tests, `typecheck:all`, design-system guards                          |
| Performance                   | measured smoke/browser baseline plus the owning hotpath contract                 |

Broad `npm test`, `npm run gate`, release, or browser lanes are reserved for changes whose scope actually crosses those boundaries.

## Stop condition

Stop restructuring when no concrete seam passes this gate. The correct next task is then a product feature, bug fix, targeted behavior test, or measured performance improvement—not another modernization stage.
