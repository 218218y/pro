# Site stays publicly reachable by link, but search engines that respect robots directives must not index it.
# This complements the HTML robots meta tags and also covers non-HTML files such as PDFs/assets.

[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet, noimageindex"
