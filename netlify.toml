# Legacy Netlify fallback only. Current production hosting is Cloudflare Pages.
# Kept so any accidental legacy Netlify deployment also remains publicly reachable by link but non-indexable.

[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow, noarchive, nosnippet, noimageindex"
